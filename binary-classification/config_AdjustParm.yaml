object:
    # 'M'-瘤体, 'P'-瘤周, 'C'-瘤内
    roi: M
    # 'A'-ADC, 'D'-DWI, 'T'-T2WI
    series: T

classifier:
    # SVM, RF, Bayes, KNN, DT, AB
    model: RF
    # LASSO, RFE, RFC, MI
    filter: RFE
    # LR, SVM
    RFE_based: SVM

save_path:
    log: '../output'